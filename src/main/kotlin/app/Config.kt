package app

object Config {

    object DatabaseConfig {
        val url = "jdbc:postgresql://localhost:5432/"
        val user = "postgres"
        val driver = "org.postgresql.Driver"
        val password = "188348"
    }
}